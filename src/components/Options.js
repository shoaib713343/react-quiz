function Options({option, dispatch, answer}) {
    return (
        <button className="btn btn-option" key={option}>
                        {option}
                    </button>
    )
}

export default Options
